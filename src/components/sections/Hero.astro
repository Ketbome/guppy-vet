---
import { Image } from "astro:assets";


---

<section id="inicio" class="hero">
	<div class="hero__bg-decoration">
		<div class="floating-shape shape-1"></div>
		<div class="floating-shape shape-2"></div>
		<div class="floating-shape shape-3"></div>
	</div>
	
	<div class="hero__container container">
		<div class="hero__content">
			<span class="hero__subtitle">
				<i class="fas fa-paw"></i>
				Bienvenidos a
			</span>
			<h1 class="hero__title">
				<span class="hero__title-accent">Guppy</span>
			
				<span class="hero__title-main">Vet</span>
			</h1>
			<p class="hero__description">
				Cuidamos a tus mascotas con <strong>amor</strong>, <strong>profesionalismo</strong> y la mejor 
				tecnología veterinaria en <em>Macul</em>. Tu compañero peludo merece el mejor cuidado médico.
			</p>
			
			<div class="hero__stats">
				<div class="stat-item">
					<div class="stat-number">10+</div>
					<div class="stat-label">Años de experiencia</div>
				</div>
				<div class="stat-item">
					<div class="stat-number">500+</div>
					<div class="stat-label">Mascotas atendidas</div>
				</div>
			</div>
			
			<div class="hero__buttons">
				<a href="https://wa.me/56992434668?text=Hola,%20me%20gustaría%20agendar%20una%20cita%20para%20mi%20mascota" class="btn btn--primary btn--large hero__btn-primary" target="_blank">
					<i class="fab fa-whatsapp"></i>
					<span>Agendar por WhatsApp</span>
					<div class="btn__shine"></div>
				</a>
				<a href="tel:+56992434668" class="btn btn--secondary btn--large hero__btn-secondary">
					<i class="fas fa-phone"></i>
					<span>Llamar Ahora</span>
				</a>
			</div>
			
			<div class="hero__trust-badges">
				<div class="trust-badge">
					<i class="fas fa-shield-alt"></i>
					<span>Certificado Profesional</span>
				</div>
				<div class="trust-badge">
					<i class="fas fa-map-marker-alt"></i>
					<span>Cerca Metro Macul</span>
				</div>
			</div>
		</div>
		
		<div class="hero__image">
			<div class="hero__image-container">
				<div class="hero__image-bg"></div>
				<div class="hero_img">
					<Image src="/guppy.webp" alt="Mascotas felices en Guppy Vet" class="hero__img" width="411" height="512" />
				</div>
				<div class="hero__floating-elements">
					<div class="floating-element element-1">
						<i class="fas fa-heart"></i>
					</div>
					<div class="floating-element element-2">
						<i class="fas fa-paw"></i>
					</div>
					<div class="floating-element element-3">
						<i class="fas fa-stethoscope"></i>
					</div>
				</div>
			</div>
			
			<div class="hero__badges">
				<div class="badge badge--primary">
					<div class="badge__icon">
						<i class="fas fa-award"></i>
					</div>
					<div class="badge__content">
						<span class="badge__number">10+</span>
						<span class="badge__label">Años</span>
					</div>
				</div>
				<div class="badge badge--secondary">
					<div class="badge__icon">
						<i class="fas fa-heart"></i>
					</div>
					<div class="badge__content">
						<span class="badge__number">500+</span>
						<span class="badge__label">Mascotas</span>
					</div>
				</div>
			</div>
		</div>
	</div>
</section>

<style>
	/* Hero Section */
	.hero {
		position: relative;
		padding: calc(var(--header-height) + 3rem) 0 5rem;
		background: linear-gradient(135deg, #f0fdfa 0%, #ecfdf5 25%, #f8fffe 50%, #edfaf9 100%);
		overflow: hidden;
	}

	/* Background Decorations */
	.hero__bg-decoration {
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		pointer-events: none;
		z-index: 1;
	}

	.floating-shape {
		position: absolute;
		border-radius: 50%;
		background: linear-gradient(135deg, rgba(16, 185, 129, 0.1), rgba(34, 197, 94, 0.05));
		animation: float 6s ease-in-out infinite;
	}

	.shape-1 {
		width: 120px;
		height: 120px;
		top: 10%;
		left: 10%;
		animation-delay: 0s;
	}

	.shape-2 {
		width: 80px;
		height: 80px;
		top: 60%;
		left: 15%;
		animation-delay: 2s;
	}

	.shape-3 {
		width: 100px;
		height: 100px;
		top: 20%;
		right: 10%;
		animation-delay: 4s;
	}

	@keyframes float {
		0%, 100% { transform: translateY(0px) rotate(0deg); opacity: 0.7; }
		50% { transform: translateY(-20px) rotate(180deg); opacity: 1; }
	}

	.hero__container {
		position: relative;
		z-index: 2;
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 5rem;
		align-items: center;
	}

	/* Content Styles */
	.hero__content {
		animation: slideInLeft 1s ease-out;
	}

	@keyframes slideInLeft {
		from { opacity: 0; transform: translateX(-50px); }
		to { opacity: 1; transform: translateX(0); }
	}

	.hero__subtitle {
		display: inline-flex;
		align-items: center;
		gap: 0.5rem;
		color: var(--primary-color);
		font-weight: 600;
		font-size: 1rem;
		text-transform: uppercase;
		letter-spacing: 0.05em;
		margin-bottom: 1rem;
		padding: 0.5rem 1rem;
		background: rgba(16, 185, 129, 0.1);
		border-radius: 50px;
		border: 1px solid rgba(16, 185, 129, 0.2);
	}

	.hero__subtitle i {
		font-size: 1.1rem;
		animation: bounce 2s infinite;
	}

	@keyframes bounce {
		0%, 20%, 50%, 80%, 100% { transform: translateY(0); }
		40% { transform: translateY(-5px); }
		60% { transform: translateY(-3px); }
	}

	.hero__title {
		font-size: 4rem;
		font-weight: 800;
		margin-bottom: 1.5rem;
		line-height: 1.1;
		text-align: left;
		width: auto;
		color: var(--text-dark);
	}

	.hero__title-accent {
		display: block;
		color: var(--primary-color) !important;
		position: relative;
		background: none !important;
		-webkit-text-fill-color: var(--primary-color) !important;
	}

	.hero__title-accent::after {
		content: '';
		position: absolute;
		bottom: -5px;
		left: 0;
		width: 100%;
		height: 4px;
		background: linear-gradient(90deg, var(--primary-color), var(--accent-color));
		border-radius: 2px;
		animation: expand 2s ease-out;
	}

	@keyframes expand {
		from { width: 0; }
		to { width: 100%; }
	}

	.hero__title-main {
		color: var(--text-dark) !important;
		text-shadow: 2px 2px 4px rgba(58, 58, 58, 0.212);
		background: none !important;
		-webkit-text-fill-color: var(--text-dark) !important;
	}

	.hero__description {
		font-size: 1.3rem;
		color: var(--text-light);
		line-height: 1.7;
		margin-bottom: 2rem;
		max-width: 90%;
	}

	.hero__description strong {
		color: var(--primary-color);
		font-weight: 700;
	}

	.hero__description em {
		color: var(--accent-color);
		font-style: normal;
		font-weight: 600;
	}

	/* Stats Section */
	.hero__stats {
		display: flex;
		gap: 2rem;
		margin-bottom: 2.5rem;
		padding: 1.5rem;
		background: rgba(255, 255, 255, 0.8);
		backdrop-filter: blur(10px);
		border-radius: 20px;
		border: 1px solid rgba(255, 255, 255, 0.3);
		box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
	}

	.stat-item {
		text-align: center;
		flex: 1;
	}

	.stat-number {
		font-size: 2rem;
		font-weight: 800;
		color: var(--primary-color);
		margin-bottom: 0.25rem;
	}

	.stat-label {
		font-size: 0.875rem;
		color: var(--text-light);
		font-weight: 500;
	}

	/* Buttons */
	.hero__buttons {
		display: flex;
		gap: 1.5rem;
		flex-wrap: wrap;
		margin-bottom: 2rem;
	}

	.hero__btn-primary {
		position: relative;
		overflow: hidden;
		background: linear-gradient(135deg, #25d366, #128c7e);
		border: none;
		transform: translateY(0);
		transition: all 0.3s ease;
	}

	.hero__btn-primary:hover {
		transform: translateY(-2px);
		box-shadow: 0 15px 30px rgba(37, 211, 102, 0.4);
	}

	.btn__shine {
		position: absolute;
		top: 0;
		left: -100%;
		width: 100%;
		height: 100%;
		background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
		transition: left 0.5s;
	}

	.hero__btn-primary:hover .btn__shine {
		left: 100%;
	}

	.hero__btn-secondary {
		background: rgba(255, 255, 255, 0.9);
		color: var(--primary-color);
		border: 2px solid var(--primary-color);
		backdrop-filter: blur(10px);
		transition: all 0.3s ease;
	}

	.hero__btn-secondary:hover {
		background: var(--primary-color);
		color: white;
		transform: translateY(-2px);
		box-shadow: 0 10px 25px rgba(16, 185, 129, 0.3);
	}

	/* Trust Badges */
	.hero__trust-badges {
		display: flex;
		gap: 1.5rem;
		flex-wrap: wrap;
	}

	.trust-badge {
		display: flex;
		align-items: center;
		gap: 0.5rem;
		padding: 0.75rem 1rem;
		background: rgba(255, 255, 255, 0.7);
		border-radius: 50px;
		border: 1px solid rgba(16, 185, 129, 0.2);
		font-size: 0.875rem;
		color: var(--text-dark);
		font-weight: 500;
	}

	.trust-badge i {
		color: var(--primary-color);
		font-size: 1rem;
	}

	/* Image Section */
	.hero__image {
		position: relative;
		animation: slideInRight 1s ease-out;
	}

	@keyframes slideInRight {
		from { opacity: 0; transform: translateX(50px); }
		to { opacity: 1; transform: translateX(0); }
	}

	.hero__image-container {
		position: relative;
		display: flex;
		justify-content: center;
		align-items: center;
	}

	.hero__image-bg {
		position: absolute;
		width: 120%;
		height: 120%;
		background: linear-gradient(135deg, var(--primary-color), var(--accent-color));
		border-radius: 50%;
		opacity: 0.1;
		animation: pulse 4s ease-in-out infinite;
	}

	@keyframes pulse {
		0%, 100% { transform: scale(1); opacity: 0.1; }
		50% { transform: scale(1.05); opacity: 0.2; }
	}

	.hero_img {
		position: relative;
		width: 400px;
		height: 400px;
		border-radius: 50%;
		overflow: hidden;
		box-shadow: 0 25px 50px rgba(0, 0, 0, 0.15);
		border: 5px solid rgba(255, 255, 255, 0.8);
		z-index: 2;
	}

	.hero__img {
		width: 100%;
		height: 100%;
		object-fit: cover;
		transform: scale(1.2);
		transition: transform 0.3s ease;
	}

	.hero_img:hover .hero__img {
		transform: scale(1.25);
	}

	/* Floating Elements */
	.hero__floating-elements {
		position: absolute;
		width: 100%;
		height: 100%;
		z-index: 3;
	}

	.floating-element {
		position: absolute;
		width: 60px;
		height: 60px;
		background: white;
		border-radius: 50%;
		display: flex;
		align-items: center;
		justify-content: center;
		box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
		border: 3px solid var(--primary-color);
		color: var(--primary-color);
		font-size: 1.5rem;
		animation: floatAround 8s ease-in-out infinite;
	}

	.element-1 {
		top: 10%;
		right: 10%;
		animation-delay: 0s;
	}

	.element-2 {
		bottom: 20%;
		left: 10%;
		animation-delay: 2s;
	}

	.element-3 {
		top: 50%;
		right: -5%;
		animation-delay: 4s;
	}

	@keyframes floatAround {
		0%, 100% { transform: translateY(0px) rotate(0deg); }
		25% { transform: translateY(-15px) rotate(90deg); }
		50% { transform: translateY(-10px) rotate(180deg); }
		75% { transform: translateY(-20px) rotate(270deg); }
	}

	/* Enhanced Badges */
	.hero__badges {
		position: absolute;
		top: 2rem;
		right: -3rem;
		display: flex;
		flex-direction: column;
		gap: 1.5rem;
		z-index: 4;
	}

	.badge {
		display: flex;
		align-items: center;
		gap: 1rem;
		background: white;
		padding: 1.5rem;
		border-radius: 20px;
		box-shadow: 0 15px 35px rgba(0, 0, 0, 0.1);
		border: 1px solid rgba(255, 255, 255, 0.8);
		backdrop-filter: blur(10px);
		transition: all 0.3s ease;
		min-width: 140px;
	}

	.badge:hover {
		transform: translateY(-5px);
		box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
	}

	.badge--primary .badge__icon {
		background: linear-gradient(135deg, var(--primary-color), var(--accent-color));
	}

	.badge--secondary .badge__icon {
		background: linear-gradient(135deg, #ef4444, #f97316);
	}

	.badge__icon {
		width: 45px;
		height: 45px;
		border-radius: 12px;
		display: flex;
		align-items: center;
		justify-content: center;
		color: white;
		font-size: 1.2rem;
	}

	.badge__content {
		display: flex;
		flex-direction: column;
		gap: 0.25rem;
	}

	.badge__number {
		font-size: 1.5rem;
		font-weight: 800;
		color: var(--text-dark);
		line-height: 1;
	}

	.badge__label {
		font-size: 0.875rem;
		color: var(--text-light);
		font-weight: 500;
	}

	/* Responsive Design */
	@media (max-width: 768px) {
		.hero {
			padding: calc(var(--header-height) + 2rem) 0 4rem;
		}

		.hero__container {
			grid-template-columns: 1fr;
			gap: 3rem;
			text-align: center;
		}

		.hero__content {
			order: 2;
		}

		.hero__image {
			order: 1;
		}

		.hero__title {
			font-size: 2.8rem;
			text-align: center;
		}

		.hero__description {
			font-size: 1.1rem;
			max-width: 100%;
		}

		.hero__stats {
			flex-direction: column;
			gap: 1rem;
			padding: 1rem;
		}

		.stat-item {
			display: flex;
			align-items: center;
			justify-content: center;
			gap: 1rem;
		}

		.stat-number {
			font-size: 1.5rem;
			margin-bottom: 0;
		}

		.hero__buttons {
			justify-content: center;
			flex-direction: column;
			align-items: center;
		}

		.hero__buttons .btn {
			width: 100%;
			max-width: 320px;
		}

		.hero_img {
			width: 280px;
			height: 280px;
		}

		.hero__badges {
			position: static;
			flex-direction: row;
			justify-content: center;
			margin-top: 2rem;
			gap: 1rem;
		}

		.badge {
			min-width: auto;
			padding: 1rem;
		}

		.badge__number {
			font-size: 1.2rem;
		}

		.floating-element {
			width: 45px;
			height: 45px;
			font-size: 1.2rem;
		}

		.hero__trust-badges {
			justify-content: center;
		}
	}

	@media (max-width: 480px) {
		.hero {
			padding: calc(var(--header-height) + 1rem) 0 3rem;
		}

		.hero__title {
			font-size: 2.2rem;
		}

		.hero__description {
			font-size: 1rem;
		}

		.hero_img {
			width: 240px;
			height: 240px;
		}

		.hero__stats {
			padding: 0.75rem;
		}

		.stat-number {
			font-size: 1.3rem;
		}

		.stat-label {
			font-size: 0.8rem;
		}

		.floating-element {
			width: 40px;
			height: 40px;
			font-size: 1rem;
		}

		.badge {
			padding: 0.75rem;
			gap: 0.75rem;
		}

		.badge__icon {
			width: 35px;
			height: 35px;
			font-size: 1rem;
		}
	}
</style>
